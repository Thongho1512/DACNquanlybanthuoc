version: "3.8"
services:
  seq:
    image: datalust/seq:latest
    container_name: seq
    ports:
      - "5341:80"
    environment:
      - ACCEPT_EULA=Y
      # For production add SEQ_FACILITY or admin credentials via UI/API
    volumes:
      - ./seq-data:/data
    restart: unless-stopped
